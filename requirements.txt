# GraphRAG — Turkish Legal Knowledge Graph
# Core
numpy>=1.24
sentence-transformers>=2.2
neo4j>=5.0
pydantic>=2.0
pydantic-settings>=2.0

# API
fastapi>=0.110
uvicorn[standard]>=0.27

# CLI / TUI (optional — only needed for tools/cli.py)
textual>=0.50

# Optional: only needed for FAISS migration (service/migrate.py)
# faiss-cpu>=1.7

# Optional: only required if using OpenAI embeddings
# openai>=1.0

# Gemini embeddings (google-genai SDK)
google-genai>=1.0
